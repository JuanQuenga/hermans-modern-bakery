import type { NextPage } from "next";
import Head from "next/head";

const Cafe: NextPage = () => {
  return (
    <>
      <Head>
        <title>Herman's Modern Bakery | Cafe</title>
      </Head>

      <section className="container mx-auto flex flex-grow flex-col md:flex-row flex-wrap">
        Cafe
      </section>
      <section className="container mx-auto flex h-screen">Test</section>
    </>
  );
};

export default Cafe;
